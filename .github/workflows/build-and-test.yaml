name: CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.1

      - name: Set up JDK 26.0.2
        uses: actions/setup-java@v5.7.0
        with:
          java-version: '26.0.2'
          distribution: zulu

      - name: Build the library
        run: |
          java --version
          chmod -R 777 gradlew
          ./gradlew clean compileJava shadowJar test

      - name: Build the library
        with: actions/upload-artifact@v7.0.1
          name: debug
          path: lib/build
